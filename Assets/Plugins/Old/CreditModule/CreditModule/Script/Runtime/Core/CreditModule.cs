using UnityEngine;


namespace CizaCreditModule
{
    public class CreditModule : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
    
}
